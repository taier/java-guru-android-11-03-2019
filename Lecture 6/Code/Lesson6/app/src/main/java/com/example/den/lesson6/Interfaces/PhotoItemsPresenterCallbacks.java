package com.example.den.lesson6.Interfaces;

public interface PhotoItemsPresenterCallbacks {
    /**
     * callback for photo items presenter
     * @param item selected item from presenter
     */
    void onItemSelected(PhotoItem item);
}
